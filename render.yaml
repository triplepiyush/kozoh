services:
  - type: web
    name: baghub-delight
    env: node
    plan: free
    buildCommand: bun install && bun run build
    startCommand: vite preview --host 0.0.0.0 --port 3000
    nodeBunVersion: 1
    envVars:
      - key: NODE_ENV
        value: production
