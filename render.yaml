services:
  - type: web
    name: baghub-delight
    env: node
    plan: free
    buildCommand: bun install && bun run build
    startCommand: bun run preview
    nodeBunVersion: 1
    envVars:
      - key: NODE_ENV
        value: production
