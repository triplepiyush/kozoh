services:
  - type: web
    name: baghub-delight
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    nodeVersion: 20
    envVars:
      - key: NODE_ENV
        value: production
